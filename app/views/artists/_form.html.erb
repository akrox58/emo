<%= simple_form_for(@artist) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :artistname %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
